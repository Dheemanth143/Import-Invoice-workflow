# 📦 Invoice Workflow Management System

This project is a full-stack application designed to manage the end-to-end workflow for processing overseas part and shipping invoices.

The system is split into two main components:
1.  **Backend API:** Built with FastAPI, handling data persistence, business logic, and file storage.
2.  **Frontend UI:** Built with Streamlit, providing portals for Overseas, Local, and Approval teams, along with a comprehensive Manager Dashboard.

## 🚀 Setup and Run Instructions

### Prerequisites

1.  Python 3.9+
2.  `pip` (Python package installer)

### 1. Clone the Repository

```bash
git clone <YOUR_REPO_URL>
cd Invoice-Workflow
